import { Stack, Center } from "../ui/EveryLayout";

const Header: React.FC = () => {
  return (
    <Center>
      <Stack>
        <h1>This is Header</h1>
      </Stack>
    </Center>
  );
};

export default Header;
